# Examples

This page provides various examples and use cases for the `AIDropDown` component.

## 🎯 Basic Examples

### Simple Text Enhancement

The most basic usage with default settings:
<iframe src="https://codesandbox.io/embed/slt76x?view=preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
     style={{ width: '100%', height: '400px', border: 'none', borderRadius: '8px' }}
     title="prompt-my-client-simple"
     allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
     sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
   ></iframe>

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function SimpleExample() {
  const [text, setText] = useState('');

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div 
    style={{ 
      display: 'flex', 
      justifyContent: 'center', 
      alignItems: 'center', 
      height: '100vh' }}>
      <div style={{ position: 'relative', width: 700 }}>
      <textarea
          ref={textareaRef}
          value={input}
          onChange={ (e) => setInput(e.target.value)}
          placeholder="Type here..."
          rows={1}
          style={{
            width: '100%',
            borderRadius: '30px',
            padding: '12px 56px 12px 16px',
            fontSize: '16px',
            border: '1px solid #ccc',
            outline: 'none',
            boxSizing: 'border-box',
            resize: 'none',
            overflow: 'hidden',
            lineHeight: '1.5',
            maxHeight: '200px',
          }}
        />
        <div style={{ position: 'absolute', top: '50%', right: 6, transform: 'translateY(-49%)' }}>
          <AIDropDown text={input} aiResponseCallback={handleAIResponse} size='38px' />
        </div>
      </div>
    </div>
  );
}
```

### Email Composition Assistant

Enhance emails with professional tone and grammar correction:

**Try it live:**

<iframe 
  src="https://codesandbox.io/embed/email-composer-demo?view=editor+%2B+preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
  style={{ width: '100%', height: '500px', border: 'none', borderRadius: '8px' }}
  title="Email Composer Demo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
/>

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function EmailComposer() {
  const [emailContent, setEmailContent] = useState('');
  const [subject, setSubject] = useState('');

  const handleAIResponse = (response: string) => {
    if (response) {
      setEmailContent(response);
    }
  };

  return (
    <div className="email-composer">
      <input
        type="text"
        value={subject}
        onChange={(e) => setSubject(e.target.value)}
        placeholder="Email Subject"
        className="subject-input"
      />
      <div className="content-area">
        <textarea
          value={emailContent}
          onChange={(e) => setEmailContent(e.target.value)}
          placeholder="Write your email content here..."
          className="email-textarea"
        />
        <div className="ai-assistant">
          <AIDropDown 
            text={emailContent} 
            aiResponseCallback={handleAIResponse}
            size="38px"
          />
        </div>
      </div>
    </div>
  );
}
```

## 🎨 Styled Examples

### Dark Theme Interface

Custom styling for a dark-themed application:

**Try it live:**

<iframe 
  src="https://codesandbox.io/embed/dark-theme-demo?view=editor+%2B+preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
  style={{ width: '100%', height: '500px', border: 'none', borderRadius: '8px' }}
  title="Dark Theme Demo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
/>

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function DarkThemeExample() {
  const [text, setText] = useState('');

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div className="dark-theme">
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="Type here..."
        className="dark-textarea"
      />
      <AIDropDown 
        text={text} 
        aiResponseCallback={handleAIResponse}
        size="52px"
        dropdownStyle={{
          background: '#1a1a1a',
          color: '#ffffff',
          borderRadius: '12px',
          border: '1px solid #333',
          boxShadow: '0 8px 32px rgba(0,0,0,0.5)'
        }}
        optionStyle={{
          padding: '16px 20px',
          fontSize: '14px',
          borderBottom: '1px solid #333',
          transition: 'all 0.2s ease'
        }}
        optionHoverStyle={{
          background: '#2a2a2a',
          color: '#00ff88',
          transform: 'translateX(8px)'
        }}
        buttonStyle={{
          background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
          border: 'none',
          boxShadow: '0 4px 15px rgba(102, 126, 234, 0.4)',
          transition: 'all 0.3s ease'
        }}
      />
    </div>
  );
}
```

### Minimalist Design

Clean, minimal styling for modern applications:

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function MinimalistExample() {
  const [text, setText] = useState('');

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div className="minimalist-container">
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="Start writing..."
        className="minimalist-textarea"
      />
      <AIDropDown 
        text={text} 
        aiResponseCallback={handleAIResponse}
        size="38px"
        dropdownStyle={{
          background: '#ffffff',
          color: '#333333',
          borderRadius: '8px',
          border: '1px solid #e1e5e9',
          boxShadow: '0 2px 8px rgba(0,0,0,0.1)'
        }}
        optionStyle={{
          padding: '12px 16px',
          fontSize: '14px',
          borderBottom: '1px solid #f0f0f0'
        }}
        optionHoverStyle={{
          background: '#f8f9fa',
          color: '#007bff'
        }}
        buttonStyle={{
          background: '#007bff',
          border: 'none',
          borderRadius: '50%',
          transition: 'all 0.2s ease'
        }}
      />
    </div>
  );
}
```

## 🔧 Advanced Examples

### Custom Prompt Options

Create your own specialized prompt options:

**Try it live:**

<iframe 
  src="https://codesandbox.io/embed/custom-prompts-demo?view=editor+%2B+preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
  style={{ width: '100%', height: '600px', border: 'none', borderRadius: '8px' }}
  title="Custom Prompts Demo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
/>

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function CustomPromptsExample() {
  const [text, setText] = useState('');

  const customPrompts = [
    {
      prompt: 'Translate to Spanish',
      label: '🇪🇸 Spanish',
      tone: 'formal',
      style: 'accurate'
    },
    {
      prompt: 'Make it rhyme',
      label: '🎵 Rhyming',
      tone: 'creative',
      style: 'poetic'
    },
    {
      prompt: 'Convert to bullet points',
      label: '📝 Bullet Points',
      tone: 'informative',
      style: 'structured'
    },
    {
      prompt: 'Make it sound like Shakespeare',
      label: '🎭 Shakespeare',
      tone: 'dramatic',
      style: 'classical'
    },
    {
      prompt: 'Summarize in one sentence',
      label: '📋 Summary',
      tone: 'concise',
      style: 'brief'
    }
  ];

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div>
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="Enter text to transform..."
        style={{ width: '100%', minHeight: '120px' }}
      />
      <AIDropDown 
        text={text} 
        aiResponseCallback={handleAIResponse}
        promptOptions={customPrompts}
        size="52px"
      />
    </div>
  );
}
```

### Multi-language Support

Handle different languages and writing styles:

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function MultiLanguageExample() {
  const [text, setText] = useState('');
  const [language, setLanguage] = useState('en');

  const languagePrompts = {
    en: [
      { prompt: 'Fix grammar', label: 'Fix Grammar' },
      { prompt: 'Make it professional', label: 'Professional' },
      { prompt: 'Make it casual', label: 'Casual' }
    ],
    es: [
      { prompt: 'Corregir gramática', label: 'Corregir Gramática' },
      { prompt: 'Hacerlo profesional', label: 'Profesional' },
      { prompt: 'Hacerlo casual', label: 'Casual' }
    ],
    fr: [
      { prompt: 'Corriger la grammaire', label: 'Corriger Grammaire' },
      { prompt: 'Rendre professionnel', label: 'Professionnel' },
      { prompt: 'Rendre décontracté', label: 'Décontracté' }
    ]
  };

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div>
      <select 
        value={language} 
        onChange={(e) => setLanguage(e.target.value)}
        style={{ marginBottom: '16px' }}
      >
        <option value="en">English</option>
        <option value="es">Español</option>
        <option value="fr">Français</option>
      </select>
      
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="Enter text..."
        style={{ width: '100%', minHeight: '100px' }}
      />
      
      <AIDropDown 
        text={text} 
        aiResponseCallback={handleAIResponse}
        promptOptions={languagePrompts[language]}
        size="48px"
      />
    </div>
  );
}
```

## 📱 Responsive Examples

### Mobile-First Design

Optimized for mobile devices:

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function MobileExample() {
  const [text, setText] = useState('');

  const handleAIResponse = (response: string) => {
    if (response) {
      setText(response);
    }
  };

  return (
    <div className="mobile-container">
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        placeholder="Type your message..."
        className="mobile-textarea"
        rows={4}
      />
      <div className="mobile-ai-button">
        <AIDropDown 
          text={text} 
          aiResponseCallback={handleAIResponse}
          size="48px"
          dropdownStyle={{
            position: 'fixed',
            bottom: '80px',
            left: '20px',
            right: '20px',
            background: '#ffffff',
            borderRadius: '16px',
            boxShadow: '0 8px 32px rgba(0,0,0,0.2)',
            zIndex: 1000
          }}
          optionStyle={{
            padding: '16px 20px',
            fontSize: '16px',
            borderBottom: '1px solid #f0f0f0'
          }}
          optionHoverStyle={{
            background: '#f8f9fa'
          }}
        />
      </div>
    </div>
  );
}
```

## 🎭 Creative Examples

### Social Media Post Generator

Generate engaging social media content:

**Try it live:**

<iframe 
  src="https://codesandbox.io/embed/social-media-demo?view=editor+%2B+preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
  style={{ width: '100%', height: '600px', border: 'none', borderRadius: '8px' }}
  title="Social Media Demo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
/>

```tsx
import { AIDropDown } from 'prompt-my-client-react';

function SocialMediaExample() {
  const [post, setPost] = useState('');
  const [platform, setPlatform] = useState('twitter');

  const socialPrompts = {
    twitter: [
      { prompt: 'Make it viral', label: '🔥 Viral' },
      { prompt: 'Add hashtags', label: '#️⃣ Hashtags' },
      { prompt: 'Make it engaging', label: '💬 Engaging' }
    ],
    instagram: [
      { prompt: 'Make it aesthetic', label: '✨ Aesthetic' },
      { prompt: 'Add emojis', label: '😊 Emojis' },
      { prompt: 'Make it inspirational', label: '🌟 Inspirational' }
    ],
    linkedin: [
      { prompt: 'Make it professional', label: '💼 Professional' },
      { prompt: 'Add industry insights', label: '📊 Insights' },
      { prompt: 'Make it thought leadership', label: '🧠 Leadership' }
    ]
  };

  const handleAIResponse = (response: string) => {
    if (response) {
      setPost(response);
    }
  };

  return (
    <div className="social-media-generator">
      <div className="platform-selector">
        <button 
          className={platform === 'twitter' ? 'active' : ''}
          onClick={() => setPlatform('twitter')}
        >
          Twitter
        </button>
        <button 
          className={platform === 'instagram' ? 'active' : ''}
          onClick={() => setPlatform('instagram')}
        >
          Instagram
        </button>
        <button 
          className={platform === 'linkedin' ? 'active' : ''}
          onClick={() => setPlatform('linkedin')}
        >
          LinkedIn
        </button>
      </div>
      
      <textarea
        value={post}
        onChange={(e) => setPost(e.target.value)}
        placeholder="Write your post content..."
        className="post-textarea"
      />
      
      <AIDropDown 
        text={post} 
        aiResponseCallback={handleAIResponse}
        promptOptions={socialPrompts[platform]}
        size="52px"
      />
    </div>
  );
}
```

## 🔄 Integration Examples

### With Form Libraries

Integrate with React Hook Form:

**Try it live:**

<iframe 
  src="https://codesandbox.io/embed/form-integration-demo?view=editor+%2B+preview&module=%2Fsrc%2FApp.js&hidenavigation=1"
  style={{ width: '100%', height: '600px', border: 'none', borderRadius: '8px' }}
  title="Form Integration Demo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
/>

```tsx
import { useForm, Controller } from 'react-hook-form';
import { AIDropDown } from 'prompt-my-client-react';

function FormIntegrationExample() {
  const { control, handleSubmit, setValue, watch } = useForm({
    defaultValues: {
      title: '',
      description: '',
      content: ''
    }
  });

  const content = watch('content');

  const handleAIResponse = (response: string) => {
    if (response) {
      setValue('content', response);
    }
  };

  const onSubmit = (data: any) => {
    console.log('Form submitted:', data);
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <Controller
        name="title"
        control={control}
        render={({ field }) => (
          <input
            {...field}
            placeholder="Title"
            className="form-input"
          />
        )}
      />
      
      <Controller
        name="description"
        control={control}
        render={({ field }) => (
          <textarea
            {...field}
            placeholder="Description"
            className="form-textarea"
          />
        )}
      />
      
      <div className="content-with-ai">
        <Controller
          name="content"
          control={control}
          render={({ field }) => (
            <textarea
              {...field}
              placeholder="Main content..."
              className="content-textarea"
            />
          )}
        />
        <AIDropDown 
          text={content} 
          aiResponseCallback={handleAIResponse}
          size="48px"
        />
      </div>
      
      <button type="submit" className="submit-button">
        Submit
      </button>
    </form>
  );
}
```

## 📚 Next Steps

- Explore the [API Reference](/docs/api) for detailed prop documentation
- Learn about [Customization](/docs/customization) options
- Check out the [Getting Started](/docs/getting-started) guide
- Visit the [GitHub Repository](https://github.com/rajanlagah/prompt-my-client-react) for source code
